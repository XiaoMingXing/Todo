import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    headerNext: {
        color: '#007aff'
    },
    container: {
        flex: 1,
        flexDirection: 'row'
    }

})

export default styles